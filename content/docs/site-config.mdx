---
title: "config/site.ts"
description: "The site config file is used to configure the site."
---

# config/site.ts

The `site.ts` file is used to configure the site. It is used to define the site name, description, and other settings.
