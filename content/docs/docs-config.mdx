---
title: "config/docs.ts"
description: "The docs config file is used to configure the docs site."
---

# config/docs.ts

The `docs.ts` file is used to configure the docs site. It is used to define the sidebar navigation, main navigation, and other settings.

## Main Nav

The `mainNav` array is used to define the main navigation items.
